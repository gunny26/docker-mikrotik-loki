# Mikrotik Logs to Loki

Python program to fetch mikrotik logs from some swith, router, gateway and send them 
via http to loki server.

i made this version, because any remote syslog stuff does not work correctly.

look in docker-compose.yml to get some idea of how to use this container.

